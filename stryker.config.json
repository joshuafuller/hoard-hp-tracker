{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Mutation testing over the pure rules domain — the correctness-critical core. `incremental` reuses prior results for unchanged mutants (only changed/affected domain code is re-tested; the full score + break threshold still hold); the incremental file is restored from CI cache, never committed (reports/ is gitignored).",
  "packageManager": "pnpm",
  "plugins": ["@stryker-mutator/vitest-runner"],
  "testRunner": "vitest",
  "coverageAnalysis": "perTest",
  "incremental": true,
  "incrementalFile": "reports/stryker-incremental.json",
  "mutate": ["src/domain/**/*.ts", "!src/domain/**/*.test.ts"],
  "reporters": ["html", "clear-text", "progress"],
  "htmlReporter": { "fileName": "reports/mutation/index.html" },
  "thresholds": { "high": 100, "low": 95, "break": 90 }
}
