{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    // Playwright specs get `test`/`expect` from the explicit @playwright/test
    // import; do NOT inherit the app's vitest/jest-dom globals (they clash).
    "types": []
  },
  "include": ["e2e/**/*.ts", "playwright.config.ts"]
}
